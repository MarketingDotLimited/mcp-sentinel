#!/usr/bin/env node
const name = process.argv[1].split('/').pop();
const args = process.argv.slice(2);
if (name === 'ufw') process.exit(1);
if (name === 'systemctl' && args.includes('start')) process.exit(1);
process.exit(0);
